spring:
    datasource:
        url: jdbc:mysql://mysql/mealddang
        username: spring
        password: spring
        driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
        hibernate:
            ddl-auto: none # 사용 안함
        properties:
            hibernate:
                '[show_sql]': false
                '[format_sql]': false